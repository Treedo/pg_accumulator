comment = 'Accumulation Registers for PostgreSQL — balance and turnover tracking'
default_version = '1.2.0'
module_pathname = '$libdir/pg_accumulator'
relocatable = false
schema = accum
requires = 'plpgsql'
superuser = true
